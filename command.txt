ssh -i dsi320 Azureuser@ipaddress

Ubuntu
    sudo apt-get update
    sudo apt install curl
    sudo apt install docker.io
    sudo apt install docker-compose
docker-compose up -d

RUN SQL
sudo docker exec -it dsi320-mysql bash
- Access Mysql
    mysql -u root -p
- MYSQL COmmads
    show databases;
    show tables;
- Create table
    show tables;
- INSERT table
- SELECT table

